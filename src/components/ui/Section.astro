---
interface Props {
  id?: string;
  className?: string;
  fullHeight?: boolean;
}

const { id, className = '', fullHeight = false } = Astro.props;
const heightClass = fullHeight ? 'min-h-screen' : '';
---

<section id={id} class={`section-container ${heightClass} ${className}`}>
  <slot />
</section>
